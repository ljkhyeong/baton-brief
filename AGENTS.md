# BATON BRIEF 작업 지침

## 작업 진행

- [HANDOFF](HANDOFF.md)의 현재 상태와 [README](README.md)의 관련 실행 방법부터 확인한다.
  동작을 바꿀 때는 [문서 색인](docs/README.md)에서 해당 PRD·ADR만 찾아 읽는다.
- 아래에서 작업에 맞는 스킬만 사용한다. 여러 영역을 바꿀 때도 필요한 부분만 함께 읽는다.
- 스킬의 권고보다 사용자의 명시적인 요청과 이미 승인한 범위를 우선한다. 되돌릴 수 있는
  구현 선택은 스스로 결정하고, 결과를 좌우하는 정보가 없을 때만 질문한다.
- 지시 때문에 작업을 멈춰야 한다면 해당 파일·문구와 중단 이유를 밝힌다.
- 코드와 문서가 다르면 실행 코드·테스트로 실제 동작을 확인하고 관련 문서를 함께 고친다.

| 변경 대상 | 스킬 |
| --- | --- |
| 이벤트 계약·멱등 수신·수신 증거 | [이벤트](.agents/skills/baton-brief-event-flows/SKILL.md) |
| 현재 관심 항목·상태 전이·주간 해소·재구축 | [투영](.agents/skills/baton-brief-projection-flows/SKILL.md) |
| 에디션 생성·이력·비교·조건부 조회 | [에디션](.agents/skills/baton-brief-edition-flows/SKILL.md) |
| 빌드·모듈·공통 설정·인증·배포 | [공통 설정](.agents/skills/baton-brief-flows/SKILL.md) |
| 문서·스킬·지시 | [문서 관리](.agents/skills/baton-brief-documentation-flows/SKILL.md) |

## 서비스와 코드 경계

- BATON은 운영 원본·사용자 권한·연속성 판정과 에디션 생성 대상·시점을 소유한다.
  사용자 조회는 BATON 백엔드를 거친다. WATCH는 URL 점검, RELAY는 구독·전달, GO는 링크 생명주기를 소유한다.
- BRIEF는 멱등 수신 기록·최초 충돌 증거·현재 관심 항목·생성 커서·불변 에디션을 소유한다.
  다른 서비스의 DB·엔티티·비밀을 공유하거나 사용자 계정·세션·자체 스케줄러를 복제하지 않는다.
- 원본 변경은 커밋 후 최소 한 번 전달되는 이벤트로 받는다. 원본 트랜잭션에 외부 호출을 묶지 않는다.
- 기술·버전은 [ADR-0002](docs/ADR/0002_technology-stack/adr.md)와 빌드 설정을 기준으로 한다.
  Kotlin/JDK 21, Spring JDBC·Flyway와 다섯 모듈을 유지하며 JPA를 추가하지 않는다.
  의존은 `bootstrap`/어댑터 → `application` → `domain`이며 어댑터끼리 의존하지 않는다.
- 기존 동작과 호출 지점을 `rg`로 찾고 Kotlin·JDK·Spring의 표준 기능을 우선 사용한다.
  이름만 바꾼 래퍼나 사용처가 없는 확장은 만들지 않는다.
- 입력 형식은 웹 DTO, 도메인 규칙은 도메인, 저장 무결성은 DB에서 검증한다.
  같은 검증의 반복은 줄이되 서로 다른 신뢰 경계의 검사는 유지한다.
- 시간 의존 코드는 `Clock`을 주입한다. AI 출력으로 원본 상태나 선정 규칙을 판정하지 않는다.
- 비밀·PII·원문 URL·길이 제한 없는 식별자를 로그나 메트릭 레이블에 넣지 않는다.

## 검증

- [검증 절차](docs/development/verification.md)에 따라 변경 범위·기존 근거·확대 조건을 먼저 확인한다.
  문서만 바꾸면 링크·형식, 테스트만 바꾸면 해당 테스트를 확인한다. 완료 관행으로 전체 검증을 추가하지 않는다.
- 작업 시작 커밋을 기록하고, 파일 수정 직후 `python3 scripts/verify-feedback.py files <경로...>`로
  해당 파일을 검사한다. 함께 수정해야 컴파일되는 파일은 한 번에 전달한다.
- 작업 종료 전 `python3 scripts/verify-feedback.py final <시작-커밋>`을 실행하고 전체 diff를 검토한다.
  중간 커밋·스테이징·미커밋·새 파일을 포함하며, 코드·빌드 변경 시 ArchUnit 구조 검사를 실행한다.
- 여러 계층의 실행 동작이나 공통 빌드·의존성을 바꾸면 최종 검토 후 `./gradlew test :bootstrap:bootJar`를
  실행한다. 계약·마이그레이션·배포 변경의 추가 검증은 해당 스킬을 따른다.
- 성공한 검증은 관련 입력·환경이 바뀌거나 새 실패 근거가 있을 때 다시 실행한다. 실패하면 원인을 구분해
  수정한 뒤 해당 실패부터 확인한다. 같은 작업 폴더에서 Gradle 검증을 중복 실행하지 않는다.
- 단순 매핑·getter·프레임워크 자체를 위한 테스트는 추가하지 않는다.
- 기존 데이터에 영향을 주는 Flyway 변경은 대표 이전 버전 행을 넣은 업그레이드로 검증한다.
  빈 DB의 마이그레이션 성공으로 대신하지 않는다.
- 실행한 명령과 확인한 환경만 성공으로 보고한다. 로컬 기동·계약 예시·공통 잠금 코드를
  각각 원격 배포·생산자 전달·별도 동시성 검증의 증거로 확대하지 않는다.
- 다음 작업에 필요한 검증 근거와 환경 제약은 HANDOFF의 해당 항목을 갱신한다. 실행 이력을 계속 덧붙이지 않는다.

## 문서·응답·Git

- 문서·주석·커밋·PR 리뷰·응답은 간단한 실무 용어로 한글 작성한다.
  코드 식별자·API·파일명·명령어·공식 기술명은 원문을 유지한다.
- 결과, 주요 변경, 검증과 남은 문제를 짧게 전달한다. 작은 수정에 고정된 장문 보고서를 붙이지 않는다.
- 기능 브랜치는 `codex/` 접두사를 쓴다. 완료한 변경은 명시적인 경로만 스테이징하고
  주된 목적에 따라 나누어 커밋한다. 제목은 `종류: 한글 요약`이며 종류는
  `기능`, `수정`, `문서`, `테스트`, `설정`, `리팩터` 중에서 고른다.
- 관련 검증을 통과한 변경만 커밋하고 실행하지 못한 검증은 최종 보고에 남긴다.
