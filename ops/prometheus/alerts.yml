groups:
  - name: brief
    rules:
      - alert: BriefMetricsUnavailable
        expr: up{job="brief"} == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: BRIEF 지표 수집이 2분 이상 실패했습니다.
      - alert: BriefServerErrors
        expr: sum(increase(http_server_requests_seconds_count{job="brief",status=~"5.."}[5m])) >= 5
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: BRIEF의 최근 5분 서버 오류가 5건 이상입니다.
