# PRD-0026: 이벤트 수신 결과 계측

- 상태: 채택됨
- 결정일: 2026-08-30
- 범위: HTTP 이벤트 수신 결과의 프로세스 내 집계

## 목적

`APPLIED`와 `APPLIED_WITH_GAP`은 모두 HTTP `202`, `DUPLICATE`와 `STALE`은 모두
HTTP `200`이다. HTTP 상태만으로 구분할 수 없는 업무 결과를 집계한다. 기존 HTTP 지연·오류,
JVM과 연결풀 계측은 Spring Boot Actuator의 자동 구성을 사용하며 직접 복제하지 않는다.

## 계측 계약

- Micrometer `Counter` 이름은 `brief.events.received`다.
- 태그는 `outcome` 하나이며 기존 `IngestStatus` 여섯 값만 사용한다.
- 웹 어댑터가 수신 유스케이스의 결과를 정상 반환받은 뒤 요청당 한 번 증가시킨다.
- 정상 적용, 공백 적용, 중복, 오래된 리비전, 충돌과 미지원 응답을 각각 집계한다.
- 같은 이벤트의 재전달도 한 요청으로 센다. 따라서 저장된 수신 기록 수나 고유 이벤트 수가
  아니며, 최초 충돌 한 건의 보존 계약을 바꾸지 않는다.
- 인증·형식 검증 거부와 처리 도중 예외는 업무 결과 카운터에 넣지 않는다. 이 실패는 기존
  HTTP 상태·오류 계측의 책임이다.
- 직접 유스케이스 호출과 투영 재구축은 HTTP 이벤트 수신 요청이 아니므로 집계하지 않는다.
- 계측은 외부 웹 어댑터에만 둔다. 도메인·application·영속성 모듈과 저장 스키마는 바꾸지 않는다.

## 보존·노출 경계

이 카운터는 영속 감사 기록이 아니다. 프로세스 재시작으로 초기화될 수 있으며 여러 인스턴스의
합산과 증가율은 향후 수집 시스템이 소유한다. 현재 수신 기록을 스캔해 카운터를 복구하지 않는다.

작업공간·시즌·이벤트 식별자, 원본 참조, fingerprint, 본문과 자격 증명을 태그에 넣지 않는다.
현재·최초 수신 시각만으로 생산자 전달 완전성이나 장애 여부를 판정하지 않는다.

PRD-0006의 health 전용 웹 노출과 Caddy 허용 목록은 그대로 유지한다. 지표 endpoint, 별도
포트, exporter, 수집 서비스와 경보는 이번 계약에서 활성화하지 않는다. 외부 수집을 도입하기
전에 비공개 접근 경로·인증·수집 주체를 결정한다. 프로세스 내 계측만으로 운영 모니터링이
완료됐다고 기록하지 않는다.

## 수용 기준

- 기존 수신 통합 시나리오에서 여섯 결과별 실제 요청 수가 각각의 카운터 증가량과 일치한다.
- 기존 수신·충돌·리비전·재구축 결과와 HTTP 상태는 바뀌지 않는다.
- `/actuator/metrics`는 기본 실행에서 노출되지 않는다.
- Micrometer 표준 API와 기존 Actuator 자동 구성을 사용하며 별도 집계 저장소·래퍼·타이머를
  만들지 않는다.

## 관련 문서

- [최소 상태 확인](../0006_minimum-health/spec.md)
- [이상 수신 증거 조회](../0011_event-receipt-anomalies/spec.md)
- [Spring Boot 지표 자동 구성](https://docs.spring.io/spring-boot/reference/actuator/metrics.html)
