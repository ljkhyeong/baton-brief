spring:
  application:
    name: baton-brief
  datasource:
    url: jdbc:postgresql://localhost:5432/baton_brief
    username: brief
    password: brief
  jackson:
    mapper:
      allow-coercion-of-scalars: false
  mvc:
    problemdetails:
      enabled: true
management:
  endpoints:
    web:
      discovery:
        enabled: false
  endpoint:
    health:
      probes:
        enabled: false
